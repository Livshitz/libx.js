{
    "extends": "./tsconfig.base.json",

    "compilerOptions": {
        "rootDirs": ["./build"],
        "baseUrl": "./"
    },

    "include": ["./src/**/*.d.ts", "./src/**/*.ts"],
    "exclude": ["**/_template.ts", "node_modules/**/*", "dist", "build", "tests"],

    "ts-node": {
        "files": true
    }
}
