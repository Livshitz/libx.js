{
    "extends": "./tsconfig.base.json",

    "compilerOptions": {
        "baseUrl": "./"
    },

    "include": ["./*.d.ts", "./src/**/*.ts"],
    "exclude": ["**/_template.ts", "node_modules/*", "dist", "build", "tests"],

    "ts-node": {
        "files": true
    }
}
