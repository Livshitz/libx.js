{
	"extends": "./tsconfig.base.json",
	// "files": [
	// 	"index.ts"
	// ],
	"include": [
		"./compiled/**/*.ts",
		"./examples/ts/**/*.ts"
		// "./**/*.ts",
		// "./compiled/libx.d.ts"
		// "./modules/**/*.ts"
	],
	"exclude": [
		"node_modules",
		"dist",
		"build",
		"tests"
	],
	"compilerOptions": {
		// "outDir": "./compiled/",
		"rootDirs": [ "./compiled" ],                        /* List of root folders whose combined content represents the structure of the project at runtime. */
		"baseUrl": "./"
	}
}