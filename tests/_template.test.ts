/*
import {  } from "module";

const mod = new mod();

beforeEach(() => {
    
});

test('-positive', () => {
	const param = { a: 1 };
	const output = mod.(param);
	expect(output).toEqual(true);
});
*/

// test('-positive', () => {
// 	const param = { a: 1 };
// 	const output = mod.(param);
// 	expect(output).toEqual(true);
// });
